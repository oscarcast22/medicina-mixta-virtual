---
interface Props {
    titulo: string;
    mapa: string;
}

const { titulo, mapa } = Astro.props
---

<div class="mapa-modal" id="mapa">
    <p>Mapa curricular {titulo} modalidad mixta-virtual</p>
    <img src={mapa} alt={`Mapa curricular de la licenciatura en ${titulo} modalidad Mixta-Virtual`} id="mapa-img" loading="eager">
</div>

<style>
    .mapa-modal p {
        text-transform: uppercase;
        font-size: var(--text-xs);
    }

    .mapa-modal img {
        width: 100%;
        cursor: pointer;

    }

    @media (max-width: 600px) {
        .mapa-modal p {
            display: none;
        }
    }
</style>
